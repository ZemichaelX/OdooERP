# -*- coding: utf-8 -*-
# Importable as a package so `models/mail_mail.py` can reach the pure module.
# It deliberately imports nothing itself: `reference/` must stay loadable
# without Odoo, which is the whole point of putting the logic there.
