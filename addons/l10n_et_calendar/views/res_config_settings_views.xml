<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.l10n.et.calendar</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="base_setup.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//block[@name='companies_setting_container']" position="after">
                <block title="Ethiopian Calendar" name="l10n_et_calendar_block">
                    <setting id="l10n_et_date_format_setting"
                             string="Ethiopian Date Display"
                             help="How Ethiopian dates are written on screen and on documents. A display choice only — the stored value is the canonical year-month-day form, so changing this rewrites nothing.">
                        <field name="l10n_et_date_format"/>
                    </setting>
                    <setting id="l10n_et_report_calendar_setting"
                             string="Calendar on Documents"
                             help="Which calendar prints on invoices and purchase orders.">
                        <field name="l10n_et_report_calendar"/>
                    </setting>
                </block>
            </xpath>
        </field>
    </record>
</odoo>
